- name: 'download and install nginx'
  apt:
    name: nginx
    state: latest
    update_cache: true
- name: 'install config file from template'
  template:
    src: nginx.conf
    dest: /etc/nginx/nginx.conf
  register: nginc.config
- name: 'start nginx service'
  service:
    name: nginx
    state: started
- name: 'create nginx container'
  docker_container:
    name: nginx
    image: nginx:alpine
    ports:
      - 81:80
    networks:
      - name: challenge-network
